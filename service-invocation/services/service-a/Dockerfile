FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py .
ENV DAPR_HTTP=http://localhost:3500
ENV TARGET_APP_ID=payments
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8001"]